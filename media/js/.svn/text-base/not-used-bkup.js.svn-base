// JavaScript Document

// clearmefocus
/*
$(document).ready(function(){
    // clear input on focus
    $('.clearMeFocus').focus(function()
    {
        if($(this).val()==$(this).attr('title'))
        {
            $(this).val('');
        }
    });

    // if field is empty afterward, add text again
    $('.clearMeFocus').blur(function()
    {
        if($(this).val()=='')
        {
            $(this).val($(this).attr('title'));
        }
    });
});
*/


// i dont know what this is
/*
    // Insert the link to confirm for those who have javascript enabled.
    $("#confirm-row").html('<a id="confirm-start" href="#">Confirm</a>');

    // Validate and bring up the confirm layer.
    $("#confirm-start").click(function(e) {
        e.preventDefault();
        var value;
        var rejected = 0;
        $(".form-row").each(function() {
            $("#confirm-values").append(
                '<p><div class="tags-left"><div>' +
                $(this).find(":first").html() + "</div></div>" +
                '<div class="tags-right"></div>'
            );
            if ($(this).find(":last").is("input:text") ||
                $(this).find(":last").is("textarea"))
            {
                value = $(this).find(":last").val();
                $("#confirm-values").append('<div>' + value + '</div>');
                if (value == "" ||
                    value == $(this).find(":last").attr("title"))
                {
                    rejected++;
                    $("#confirm-values").html("");
                    $.jGrowl("Fill in " + $(this).find(":first").html());
                }
            }
            else if ($(this).find(":last").is("input:radio"))
            {
                value = $(this).find("input:radio:checked").parent().text();
                $("#confirm-values").append('<div>' + value + '</div>');
                if (value == "")
                {
                    rejected++;
                    $("#confirm-values").html("");
                    $.jGrowl("Choose a " + $(this).find(":first").html());
                }
            }
            $(this).find("#confirm-values").append("</p>");
        });
        // Show if values ok. Otherwise show error message.
        if (rejected == 0)
        {
            $("#content-confirm").show();
        }
        else
        {
            $("#confirm-values").html("");
        }
    });

    // Destroy the confirm layer.
    $("#confirm-cancel").click(function(e) {
        e.preventDefault();
        $("#confirm-values").html("");
        $("#content-confirm").hide();
    });

//this is the widget sticky script
window.onscroll = function()
{
    if( window.XMLHttpRequest ) {
        if (document.documentElement.scrollTop > 108 || self.pageYOffset > 108) {
            $('#widget-right').css('position','fixed');
            $('#widget-right').css('top','0');
        } else if (document.documentElement.scrollTop < 108 || self.pageYOffset < 108) {
            $('#widget-right').css('position','absolute');
            $('#widget-right').css('top','108px');
        }
    }
};
*/
